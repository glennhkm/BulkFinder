name: bulk_finder
description: "Bulk Finder"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter    
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.7
  cupertino_icons: ^1.0.8
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/
  uses-material-design: true